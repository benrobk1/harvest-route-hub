# Supabase Configuration (Auto-generated by Lovable Cloud)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
VITE_SUPABASE_PROJECT_ID=your_project_id

# Testing (Required for load tests only)
# Get this from your Supabase project settings -> API -> service_role key
# WARNING: Never commit this key to version control!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Optional: Add these for extended functionality
# RESEND_API_KEY=your_resend_api_key (for email notifications)
# MAPBOX_PUBLIC_TOKEN=your_mapbox_token (for geocoding and routing)

# Stripe Configuration
# VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key (Frontend - for Stripe.js)

# BACKEND ONLY - Never expose these in frontend!
# These secrets should ONLY be used in Supabase Edge Functions
# STRIPE_SECRET_KEY=your_stripe_secret_key (Backend only - for Stripe API)
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret (Backend only - for webhook verification)

# CRITICAL SECURITY NOTES:
# - Only VITE_* variables are accessible in frontend code
# - STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET must NEVER be exposed to the client
# - These backend secrets should only be used in supabase/functions/* edge functions
# - Never log, commit, or include these secrets in client-side code

# Error Tracking & Monitoring (Highly Recommended for Production)
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id (Frontend error tracking)
# SENTRY_ORG=your-org-slug (For source maps upload during build)
# SENTRY_PROJECT=blue-harvests (For source maps upload during build)
# SENTRY_AUTH_TOKEN=your-sentry-auth-token (For source maps upload during build)
